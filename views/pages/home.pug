extends ../base.pug

block variables
    - var template = "home"

block content
    .home
        .home__wrapper
            h1.home__title Home Page
            img(src="assets/image.jpg", alt="", srcset="")